# Modelo de datos · 001-agentes-codex

No aplica. La funcionalidad no persiste datos ni modifica esquemas.

La única invariante estructural es una relación uno a uno entre cada nombre canónico de
`.claude/agents/` y un adaptador de Codex con el mismo `name`.
